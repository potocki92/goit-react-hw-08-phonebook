import React from 'react';
import InputsForm from 'components/InputsForm/InputsForm';

const LoginForm = () => {
  return (
    <InputsForm buttonText="Log In" formType="login" />
  );
};

export default LoginForm;
